# PR #2 Review Notes — feat: build Ember RFC tracker with hexagonal architecture

**Date:** 2026-04-15
**PR:** trev-gulls/rfc-embers#2
**Reviewers:** trev-gulls (human), automated agents (code, tests, errors, types)

---

## Human Reviewer Feedback (trev-gulls)

All three inline comments were addressed and replied to in commit `c1a9cfe`:

- `@types/ember__*` packages removed — Ember 5 + Glint ships its own types
- Optional `ember-cli-*` packages trimmed (sri, welcome-page, broccoli-asset-rev)
- `webpack` presence explained — peer dep of `ember-auto-import`, not a direct choice

---

## Agent Review Findings

### Fixed in This PR

| # | Source | Finding |
|---|--------|---------|
| F1 | Code | **EmberData version mismatch** — `ember-data@~5.3.8` vs `@ember-data/*@^5.8.1`. Align all to same version. |
| F2 | Code | **Dead `podModulePrefix`** in `app/app.ts:9` — `config/environment.js` never sets it; delete the line. |
| F3 | Code | **`@ember-data/*` in `dependencies`** instead of `devDependencies` — Ember app convention. Move to devDeps. |
| F4 | Code | **Acceptance tests import `setupApplicationTest` from `ember-qunit`** directly, bypassing `tests/helpers`. |

### Outstanding Issues (Track as GitHub Issues)

| # | Severity | Source | Finding |
|---|----------|--------|---------|
| O1 | Critical | Errors | **No error substates** — `rfcs-error.hbs` / `rfc-error.hbs` missing; API failures produce a blank page |
| O2 | Critical | Errors | **No fetch timeout** in `GitHubRfcSource` — network stall hangs the app forever |
| O3 | Critical | Errors | **`response.json()` unguarded** — malformed response surfaces as opaque `SyntaxError` with no context |
| O4 | Important | Code | **`fetchAll` params ignored** — gateway interface promises filtering no implementation delivers; remove `params` or implement |
| O5 | Important | Tests | **`GitHubRfcSource.fetchOne` untested** — separate endpoint, separate JSON:API shape, zero coverage |
| O6 | Important | Tests | **`RfcsController.filteredRfcs` untested in isolation** — acceptance tests only cover one status |
| O7 | Important | Tests | **`github-handle` → `githubHandle` serializer path untested** — silent breakage if EmberData changes normalization |
| O8 | Important | Errors | **`RfcAdapter#gateway` unsafe cast** — DI misconfiguration produces misleading `is not a function` errors |
| O9 | Important | Errors | **`GitHubIssue.user` not nullable** — GitHub returns `null` for deleted accounts; crashes entire `fetchAll` |
| O10 | Important | Types | **`JsonApiDocument.data` ambiguous union** — split into `JsonApiSingularDocument` / `JsonApiCollectionDocument`; removes `@ts-expect-error` in adapter |
| O11 | Important | Types | **`ALL_STATUSES` manually maintained** — convert `RfcStatus` to `const RFC_STATUSES = [...] as const` and derive type from it |
| O12 | Suggestion | Types | **`Author.name` / `githubHandle` / `id` triple-alias** — all set to `login`; misleading model |
| O13 | Suggestion | Errors | **`mapStatus` fallthrough undocumented** — "no label" silently maps to `'proposed'` |
| O14 | Suggestion | Errors | **`per_page=100` silent truncation** — warn if `issues.length === 100`; real repos have >100 issues |
| O15 | Suggestion | Errors | **No loading substates** — `rfcs-loading.hbs` / `rfc-loading.hbs` missing |
| O16 | Suggestion | Errors | **`rfc_id` not validated before API call** — `/rfcs/banana` fires a 404 fetch |
| O17 | Suggestion | Errors | **GitHub API rate limiting** — 60 req/hr unauthenticated; document limitation or add token support |
